# Skill Blueprint: {领域名称}

> 本文件由 ssot-prompt-engineer skill 自动生成，作为 skill-creator 的输入。
> 请勿手动修改 schema 结构，内容可按需调整。

## Meta
- schema_version: 1.0
- 文档类型: {概要设计说明书 / 测试报告 / 投标方案 / ...}
- 目标受众: {外部客户 / 内部团队 / 监管机构 / ...}
- 来源样本: {文件名列表}
- 提取日期: {YYYY-MM-DD}

---

## 1. 目标定义

{一段话描述：这个 skill 帮用户生成什么文档，用于什么业务场景，面向谁，核心价值是什么}

---

## 2. 输入参数表

| 参数 | 必填 | 类型 | 说明 | 示例 |
|------|------|------|------|------|
| {参数名} | 是/否 | 文本/表格/列表/文件 | {描述} | {具体示例} |

**类型说明：**
- `文本`：短文本，直接提问收集
- `表格`：结构化数据，提供表格模板让用户填写
- `列表`：多项条目，逐条收集或批量输入
- `文件`：要求用户上传文件

---

## 3. 输出结构定义

{完整章节骨架，每个节点标注分类}

```
1  {章标题}                          [固定模板|半模板|项目特定]
   1.1 {节标题}                      [固定模板|半模板|项目特定]
       1.1.1 {小节标题}              [固定模板|半模板|项目特定]
```

**分类标注取值：**
- `[固定模板]` — 内容不随项目变化，可直接复用
- `[半模板]` — 结构/框架固定，部分细节随项目变化
- `[项目特定]` — 完全依赖用户输入生成

---

## 4. 固定模板内容

> skill-creator 处理方式：写入 `references/` 目录作为模板文件。

### 模板: {章节编号} {节名称}

~~~
{完整模板文本，仅含 {项目名称} 等极少占位符}
~~~

---

## 5. 半模板规则

> skill-creator 处理方式：固定结构写入 `references/`，变量部分在 `SKILL.md` 中定义生成规则。

### {章节编号} {节名称}

- **固定结构**: {哪些元素/段落/列/顺序是不变的}
- **变量部分**: {哪些内容随项目变化}
- **生成模式**: {写作公式、句式模板、或条件逻辑}
- **样本摘录**:
  > {从原始样本中摘取的实际内容，作为 few-shot 示例}

---

## 6. 项目特定规则

> skill-creator 处理方式：在 `SKILL.md` 中定义详细生成指令，关联参数表中的具体参数。

### {章节编号} {节名称}

- **依赖参数**: {对应参数表中的参数名}
- **格式约束**: {表格列定义 / 列表格式 / 段落结构}
- **写作规范**: {用词模式、句式公式、数量要求}
- **样本摘录**:
  > {从原始样本中摘取的实际内容，作为 few-shot 示例}

---

## 7. 写作风格约束

| 维度 | 规范 |
|------|------|
| 人称 | {第一人称复数/第三人称/无主语被动句} |
| 语气 | {正式/半正式} |
| 句式 | {短句为主/长句/混合} |
| 术语 | {首次出现缩写需给全称/...} |
| 编号 | {阿拉伯数字/最多N级} |
| 表格 | {首行表头加粗/...} |
| 图注 | {图后居中标注/...} |
| 引用 | {书名号《》包裹文件名/...} |

---

## 8. 质量检查项

### 完整性
- [ ] {所有章节是否齐全}

### 一致性
- [ ] {跨章节数据是否一致}

### 可追溯性
- [ ] {输出是否可追溯到输入}

### 格式
- [ ] {是否符合写作风格约束}
